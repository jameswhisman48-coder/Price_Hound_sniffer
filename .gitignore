node_modules

.env
.env.local
.DS_Store
.cache
.output
.nitro
.tanstack

/dist/
/dist-ssr/

# Vercel Build Output API bundle (produced by build-vercel.sh at go-live).
/.vercel/

# Runtime server log written on start.
/.run/
# Captured waitlist signups (local disk). Never commit subscriber data.
/data/

# Generated by the TanStack Start router plugin on dev/build.
src/routeTree.gen.ts

# Local databases and logs (never commit runtime data or secrets).
*.db
*.sqlite
*.sqlite3
*.log
.env.*
!.env.example
